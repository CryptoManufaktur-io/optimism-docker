# To be used with optimism.yml
services:
  op-geth:
    ports:
      - ${RPC_PORT:-8545}:${RPC_PORT:-8545}/tcp
      - ${WS_PORT:-8546}:${WS_PORT:-8546}/tcp
  op-node:
    ports:
      - ${OPNODE_RPC_PORT:-9545}:${OPNODE_RPC_PORT:-9545}/tcp